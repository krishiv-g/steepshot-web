# Environment variables for Create React App
# These will be embedded into the build at compile time

# Public URL for assets (leave empty for development)
PUBLIC_URL=

# Disable OneSignal in development to prevent origin errors
REACT_APP_DISABLE_ONESIGNAL=true

# Node environment (automatically set by npm scripts, but can override for testing)
# NODE_ENV=development
